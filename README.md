You need to write a function to create a new array that will contain no more positive numbers. For whom:

Let's create a function with a suitable name.
The function accepts an array as a parameter.
In this function, an empty array is created (example: const exampleArr = []).
If the array passed as a parameter to a function is empty, then the reporting is rotated.
We loop through the array passed as a parameter (using the for loop).
In the body of the loop, we check whether the current element is a positive number or a positive number (if).
If the number is positive, then we add it to the previously created array using the additional push function (example exampleArr.push(currentPositiveNumber)).
After completing the cycle, the array is checked and filled with positive values.
Since the wine is not empty, we turn the whole array.
If it is empty, we turn null.
