This task consists of four small tasks, for the implementation of each task you can deduct 25 points:

An array with elements of different types is given. Create a function as the Virach arithmetic mean of the numerical elements of this array.
Write a function doMath(x, znak, y), which takes 3 arguments: numbers x and y, row znak. The changeable sign can have: +, -, *, /, %, ^ (step). Display the result of the mathematical operation specified in the changeable sign. The two numbers and the sign come out as a correspondent.
Write a function for filling the data of a domestic array with the data of a home array. The content of the main array and internal arrays is set by the user. The values ​​of all elements of all arrays are specified by the user.
Create a function that puts in order all the characters passed as another argument. 'func(" hello world", ['l', 'd'])' gives us "heo wor". The outgoing row and symbols for the selection are specified by the correspondent.
